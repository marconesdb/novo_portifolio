import styled from 'styled-components';

export const ContentWrapper = styled.div`
  flex: 1; /* Ocupa o espaço restante disponível no Container */
  display: flex;
  flex-direction: column; /* Organiza os filhos em coluna */
  width: 100%; /* Garante que o ContentWrapper ocupe a largura total disponível */
`;
